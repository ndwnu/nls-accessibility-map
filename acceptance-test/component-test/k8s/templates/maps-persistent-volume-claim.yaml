kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: azure-maps-blob-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: azurefile
