kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: traffic-sign-azure-blob-claim
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: azurefile
