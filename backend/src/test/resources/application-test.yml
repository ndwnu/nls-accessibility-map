nls:
  springboot:
    test:
      main:
        url: http://localhost:8080
        await:
          enabled: true
          path: /actuator/health
          duration: PT1M
          description: accessibility-map-api
